#!/usr/bin/env bash
set -euo pipefail

SCRIPT_PATH="${BASH_SOURCE[0]}"

while [ -L "$SCRIPT_PATH" ]; do
  SCRIPT_DIR="$(cd "$(dirname "$SCRIPT_PATH")" && pwd)"
  SCRIPT_PATH="$(readlink "$SCRIPT_PATH")"
  case "$SCRIPT_PATH" in
    /*) ;;
    *) SCRIPT_PATH="${SCRIPT_DIR}/${SCRIPT_PATH}" ;;
  esac
done

REPO_DIR="$(cd "$(dirname "$SCRIPT_PATH")/.." && pwd)"
# shellcheck disable=SC1091
. "$REPO_DIR/lib/modules/bootstrap.sh"
dev_kit_bootstrap

if [ -f "$REPO_DIR/lib/modules/utils.sh" ]; then
  # shellcheck disable=SC1090
  . "$REPO_DIR/lib/modules/utils.sh"
fi

for module_file in "$REPO_DIR"/lib/modules/*.sh; do
  [ "$module_file" = "$REPO_DIR/lib/modules/bootstrap.sh" ] && continue
  [ "$module_file" = "$REPO_DIR/lib/modules/utils.sh" ] && continue
  # shellcheck disable=SC1090
  . "$module_file"
done

for command_file in "$REPO_DIR"/lib/commands/*.sh; do
  # shellcheck disable=SC1090
  . "$command_file"
done

usage() {
  local command_name=""
  local description=""
  cat <<'EOF'
Usage: dev.kit <command>

Commands:
EOF

  while IFS= read -r command_name; do
    description="$(dev_kit_command_description "$(dev_kit_command_file_path "$REPO_DIR" "$command_name")")"
    printf "  %-10s %s\n" "$command_name" "$description"
  done <<EOF
$(dev_kit_public_command_names)
EOF

  cat <<'EOF'
  help       Show this help message

Options:
  --json     Output machine-readable JSON for supported commands
EOF
}

command_usage() {
  local command_name="$1"
  echo "Usage: dev.kit ${command_name} [options]"
  echo
  echo "Options:"
  case "$command_name" in
    action)
      echo "  --json     Output machine-readable JSON"
      echo "  --dev      Evaluate the local development workflow"
      echo "  --ci       Evaluate through push readiness without pull request creation"
      echo "  --pr       Evaluate through pull request creation and review follow-up"
      echo "  --refresh-context  Regenerate optional repo-local continuity files"
      echo "  --yes      Overwrite existing repo-local continuity files without a confirmation prompt"
      ;;
    explore|learn|status)
      echo "  --json     Output machine-readable JSON"
      ;;
    uninstall)
      echo "  --yes      Remove dev.kit without a confirmation prompt"
      ;;
  esac
}

command="${1:-explore}"
format="text"
command_args=()

if [ "$#" -gt 0 ]; then
  shift
fi

while [ "$#" -gt 0 ]; do
  case "$1" in
    --json)
      format="json"
      ;;
    *)
      command_args+=("$1")
      ;;
  esac
  shift
done

if [ "$command" = "--json" ]; then
  command="explore"
  format="json"
fi

case "$command" in
  audit|bridge|sync)
    command="action"
    ;;
  save)
    command="action"
    command_args=(--refresh-context "${command_args[@]}")
    ;;
esac

case "$command" in
  help|-h|--help)
    usage
    ;;
  *)
    fn="dev_kit_cmd_${command//-/_}"
    if command -v "$fn" >/dev/null 2>&1; then
      if [ "${command_args[0]:-}" = "-h" ] || [ "${command_args[0]:-}" = "--help" ]; then
        command_usage "$command"
        exit 0
      fi
      "$fn" "$format" "${command_args[@]}"
      exit 0
    fi
    echo "Unknown command: $command" >&2
    echo >&2
    usage >&2
    exit 1
    ;;
esac
