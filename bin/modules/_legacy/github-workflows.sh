#!/bin/bash

# GitHub Workflows module for configure github actions workflows (from udx/reusable-workflows templates repository) and guide on env/secrets configuration, cloud providers auth configuration (keyless oidc)
# Features:
# - set github actions workflows
# - update github actions workflows
# - guide on env/secrets configuration, cloud providers auth configuration (keyless oidc)

