# Workflow Repo

## Usage

This repository publishes reusable GitHub workflow contracts.

## Architecture

Workflow contracts live in `.github/workflows/` and documentation lives in `README.md`.

## Development

Run verification with:

```bash
npm test
```

Environment inputs are documented in the workflow interface and examples.
