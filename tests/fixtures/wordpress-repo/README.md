# WordPress Site

## Usage

Run local verification with:

```bash
npm test
```

## Architecture

The site lives in `wp-content/` and uses MU plugins for bootstrap behavior.

## Development

Environment variables and workflow inputs are documented separately.
