# Todo

Generated by `dev.kit action --refresh-context`. Repo changes can stale this context; rerun `dev.kit action --refresh-context --yes` after docs, workflows, configs, entrypoints, or structure change.

## Next Session Start

- Read `./.udx/dev.kit/context.md`
- Read `./.udx/dev.kit/refs.md`
- Run verification: `{{verify_cmd}}`
- Reconfirm runtime entrypoint: `{{runtime_cmd}}`
- Reconfirm build entrypoint: `{{build_cmd}}`

## Suggested Repo Improvements

{{advice_lines}}

## Working Notes

- Record the current task, blockers, and intended next change here.
- Move durable process knowledge into the repo itself so the next `dev.kit action --refresh-context` gets better automatically.
- Refresh this file after repo changes so AI consumers read current context instead of stale carry-over notes.
