{
  "command": "{{command}}",
  "repo": "{{repo}}",
  "workflow": "{{workflow}}",
  "mode": "{{mode}}",
  "behavior": "{{behavior}}",
  "description": "{{description}}",
  "next_hint": "{{next_hint}}",
  "repo_state": {{repo_state}},
  "capabilities": {{capabilities}},
  "steps": {{steps}}
}
