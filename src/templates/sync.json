{
  "command": "{{command}}",
  "repo": "{{repo}}",
  "workflow": "{{workflow}}",
  "mode": "{{mode}}",
  "behavior": "{{behavior}}",
  "description": "{{description}}",
  "capabilities": {{capabilities}},
  "steps": {{steps}}
}
