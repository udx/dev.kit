{
  "command": "{{command}}",
  "repo": "{{repo}}",
  "workflow": {
    "id": "{{workflow_id}}",
    "name": "{{workflow_name}}"
  },
  "mode": "{{mode}}",
  "behavior": "{{behavior}}",
  "description": "{{description}}",
  "next_hint": "{{next_hint}}",
  "repo_state": {{repo_state}},
  "hooks": {{hooks}},
  "capabilities": {{capabilities}},
  "steps": {{steps}}
}
