# Work items

## Default flow
1) Confirm scope and source of truth.
2) Implement scripts, then docs, then reports.
3) Validate with read-only checks.
4) Commit small, cohesive changes.

## 2026-01-28
1) Wire new workflow input output by adding step `id` and output mapping.
2) Update infra-build docs to mention new input and default.
3) Update example workflow to show version pinning (e.g., `4.8.0`).
