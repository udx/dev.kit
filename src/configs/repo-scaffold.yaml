kind: repoScaffold
version: udx.io/dev.kit/v1
config:

  # Scaffold applied to every repo regardless of archetype
  baseline:
    dirs:
      - docs
      - .github/ISSUE_TEMPLATE
    files:
      - .gitignore
      - AGENTS.md
      - .github/PULL_REQUEST_TEMPLATE.md

  # Additional scaffold items per archetype
  archetypes:
    shell-cli:
      dirs:
        - bin
        - lib/commands
        - lib/modules
        - src/configs
        - src/templates
        - tests
      files:
        - Makefile
        - README.md

    node-app:
      dirs:
        - src
        - tests
      files:
        - package.json
        - README.md

    runtime-image:
      dirs:
        - bin
        - tests
      files:
        - Dockerfile
        - Makefile
        - README.md

    wordpress-site:
      dirs:
        - docs
      files:
        - README.md

    infra-pipeline:
      dirs:
        - .github/workflows
        - terraform
        - docs
      files:
        - README.md

  # Factor-driven scaffold: what to create when a factor is missing or partial
  factors:
    documentation:
      files:
        - README.md
        - docs/integration.md
        - docs/context.md
    config:
      files:
        - .env.example
    pipeline:
      dirs:
        - tests
        - .github/workflows
      files:
        - Makefile
