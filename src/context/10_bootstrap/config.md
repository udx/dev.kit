# UDX DevKit config

## Overview

- Default and auto enabled on install
- Flexible configuration experience
  - Interactive (user experience)
  - Programmatic (input params)
  - Manual (config file)

### Shell startup

- Integrates `dev.kit` module into shell startup
  - Auto-detect and enable all by default
