# Local Tooling Config (Source)

Scope
- Defines config schema for dev.kit local settings.
- Used by config workflows to drive interactive prompts.

Schema
- `config-schema.json` is the source schema.
