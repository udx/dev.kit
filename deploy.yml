kind: workerDeployConfig
version: udx.io/worker-v1/deploy

config:
  image: "usabilitydynamics/udx-worker:latest"
  name: "dev-kit-test-suite"
  volumes:
    - ".:/workspace"
  env:
    TERM: "xterm-256color"
  command: "/bin/bash"
  args:
    - "/workspace/tests/suite.sh"

